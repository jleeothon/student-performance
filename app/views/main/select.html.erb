<%= form_tag query_path, method: 'get' do %>
  <div>
    <%= label_tag 'column', 'What do you want to diagnose or predict?' %>
    <%= select_tag 'column', options_from_collection_for_select(@options, :name, :desc)  %>
  </div>
  <div>
    <%= submit_tag 'Go!' %>
  </div>
<% end %>
